<h3><em><p class="text-info">Entrar</p></em></h3>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div><%= f.label :email %>
  <%= f.email_field :email, :autofocus => true %></div><br/>

  <div><%= f.label :contraseña %>
  <%= f.password_field :password %></div>

  <% if devise_mapping.rememberable? -%>
    <div><%= f.check_box :remember_me %> <%= f.label :remember_me, "Recordarme" %></div><br/>
  <% end -%>

  <div><%= f.submit "Entrar", :class=>"btn btn-success" %></div>
<% end %>

<%= render "devise/shared/links" %>
